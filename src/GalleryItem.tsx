/*
function GalleryItem({image, link}){
    const content = (
        <div className="box">
            <img src={image} alt ="" />
        </div>
    );

    return link ? <a href={link}>{content}</a> : content;
}

export default GalleryItem;
*/